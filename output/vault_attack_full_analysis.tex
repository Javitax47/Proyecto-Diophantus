
\documentclass[10pt, a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{geometry}
\geometry{a4paper, margin=0.8in} 
\usepackage{amssymb}
\usepackage{breqn} 
\usepackage{booktabs}
\usepackage{url}

% Ajustes para permitir ecuaciones gigantes
\allowdisplaybreaks
\setlength{\jot}{8pt}

\title{Análisis Matemático de Algoritmo \\ \large Generado por Project Diophantus}
\author{}
\date{\today}

\begin{document}
\maketitle
\section*{Resumen Ejecutivo}
Este documento presenta la traducción completa de un algoritmo computacional a tres formas matemáticas distintas.
Las ecuaciones resultantes, debido a la complejidad logarítmica del algoritmo original (Miller-Rabin), son extensas pero polinomialmente acotadas.
\part{Función de Transición}
\begin{align*}
attacker\_donation[t+1] &= attacker_donation \\
total\_assets[t+1] &= total_assets \\
total\_supply[t+1] &= total_supply \\
victim\_deposit[t+1] &= victim_deposit \\
victim\_shares[t+1] &= ('+', ('+', 'convertToShares', 'total_assets'), 'victim_deposit') \\
\end{align*}\part{Conversión a Polinomio}
\subsection*{Ecuación Unificada P=0}
\begin{align*}
& (attacker_donation[t+1] - (attacker_donation))^{2} \\
& + (total_assets[t+1] - (total_assets))^{2} \\
& + (total_supply[t+1] - (total_supply))^{2} \\
& + (victim_deposit[t+1] - (victim_deposit))^{2} + (e_{0} - (convertToShares \\
& + total_assets))^{2} + (victim_shares[t+1] - (e_{0} + victim_deposit))^{2} = 0
\end{align*}\part{Fórmulas Generadoras}
\begin{small}
\begin{align*}
G &= 1 - ((attacker_donation[t+1] - (attacker_donation))^{2} \\
& + (total_assets[t+1] - (total_assets))^{2} \\
& + (total_supply[t+1] - (total_supply))^{2} \\
& + (victim_deposit[t+1] - (victim_deposit))^{2} + (e_{0} - (convertToShares \\
& + total_assets))^{2} + (victim_shares[t+1] - (e_{0} + victim_deposit))^{2}) \\
\end{align*}
\end{small}\part{Fórmulas Matemáticas (Estilo Willans)}
\section{Representación Simbólica}
\begin{dmath*}
f(N) &= \sum_{R=0}^{\infty} R \cdot \lfloor \frac{1}{1 + \mathcal{D}_{sym}} \rfloor \\
\mathcal{D}_{sym} &= (\mathit{attacker\_donation} - (\mathit{attacker\_donation}))^2 + (\mathit{total\_assets} - (\mathit{total\_assets}))^2 + \\
&  (\mathit{total\_supply} - (\mathit{total\_supply}))^2 + (\mathit{victim\_deposit} - (\mathit{victim\_deposit}))^2 + \\
&  (\epsilon_{0} - (\mathit{convertToShares} + \mathit{total\_assets}))^2 + (\mathit{victim\_shares} - (\epsilon_{0} + \mathit{victim\_deposit}))^2
\end{dmath*}\newpage
\section{Representación Operacional}
\begin{dmath*}
f(N) &= \sum_{R=0}^{\infty} R \cdot \left\lfloor \frac{1}{1 + \mathcal{D}(N, R, \mathbf{x})} \right\rfloor \\
\mathcal{D}(N, R, \mathbf{x}) &= \sum_{\mathbf{x} \in \mathbb{N}^{7}} \left( (x_{1} - (x_{1}))^2 + (x_{6} - (x_{6}))^2 + (x_{5} - (x_{5}))^2 + (x_{3} - (x_{3}))^2 + (x_{7} - (x_{2} + x_{6}))^2 + (x_{4} - (x_{7} + x_{3}))^2 \right)
\end{dmath*}
Vector de estado: $\mathbf{x} \in \mathbb{R}^{7}$ (variables internas anonimizadas).\\\part{Sistema Dinámico (Recurrencia Infinita)}
\begin{align*}
\text{Sistema de recurrencia generado (Placeholder)} \\
\end{align*}\end{document}